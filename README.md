React Video Player (Vite + HLS)

🚀 Run locally

yarn
yarn dev

Open: http://localhost:5173

🧩 Architecture Note

The player is currently implemented as a single component due to limited time.
In a production scenario, it would be better to split it into smaller components (controls, timeline, tooltip, etc.), but for now the complexity doesn’t require it.
